version: 2
before:
  hooks:
    - go mod download
    - go mod tidy
builds:
  - id: rrsh_linux_amd64
    binary: rrsh
    env:
      - CGO_ENABLED=0
    ldflags:
      - "-s -w -X main.version={{.Version}} -X main.commit={{.Commit}} -X main.date={{.Date}}"
    goos: [ linux ]
    goarch: [ amd64 ]
  - id: rrsh_linux_armv6
    binary: rrsh
    env:
      - CGO_ENABLED=0
    ldflags:
      - "-s -w -X main.version={{.Version}} -X main.commit={{.Commit}} -X main.date={{.Date}}"
    goos: [ linux ]
    goarch: [ arm ]
    goarm: [ 6 ]
  - id: rrsh_linux_armv7
    binary: rrsh
    env:
      - CGO_ENABLED=0
    ldflags:
      - "-s -w -X main.version={{.Version}} -X main.commit={{.Commit}} -X main.date={{.Date}}"
    goos: [ linux ]
    goarch: [ arm ]
    goarm: [ 7 ]
  - id: rrsh_linux_arm64
    binary: rrsh
    env:
      - CGO_ENABLED=0
    ldflags:
      - "-s -w -X main.version={{.Version}} -X main.commit={{.Commit}} -X main.date={{.Date}}"
    goos: [ linux ]
    goarch: [ arm64 ]
nfpms:
  - package_name: rrsh
    homepage: https://github.com/binwiederhier/rrsh
    maintainer: Philipp C. Heckel <philipp.heckel@gmail.com>
    description: Restricted login shell that allowlists commands via YAML
    license: Apache 2.0
    formats:
      - deb
      - rpm
    bindir: /usr/bin
    contents:
      - src: rrsh.json.example
        dst: /etc/rrsh/rrsh.json
        type: "config|noreplace"
archives:
  - id: rrsh_linux
    ids:
      - rrsh_linux_amd64
      - rrsh_linux_armv6
      - rrsh_linux_armv7
      - rrsh_linux_arm64
    wrap_in_directory: true
    files:
      - LICENSE
      - README.md
      - rrsh.json.example
checksum:
  name_template: 'checksums.txt'
snapshot:
  version_template: "{{ .Tag }}-next"
changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
